# 🎓 University Portal - Портал кафедры IT

Полноценный Full-Stack проект веб-портала кафедры информационных технологий университета с современным дизайном и всеми CRUD функциями.

## 🚀 Технологии

### Frontend
- React 18 + Vite
- TailwindCSS
- React Router v6
- Zustand (State Management)
- Axios
- Chart.js
- Lucide React Icons
- React Hot Toast

### Backend
- Node.js + Express
- Prisma ORM
- PostgreSQL
- JWT Authentication
- bcryptjs
- CORS

## ✨ Возможности

### 🔐 Авторизация
- JWT токены
- 3 роли: ADMIN, TEACHER, STUDENT
- Защищенные маршруты

### 👨‍💼 Администратор
- ✅ Управление преподавателями (CRUD)
- ✅ Управление студентами (CRUD)
- ✅ Управление предметами (CRUD)
- ✅ Управление учебными планами (CRUD)
- ✅ Управление новостями (CRUD)
- ✅ Управление выпускниками (CRUD)
- ✅ Статистика и аналитика
- ✅ Топ-10 студентов
- ✅ Диаграммы

### 👨‍🏫 Преподаватель
- ✅ Просмотр своих предметов
- ✅ Добавление/редактирование/удаление ТОЛЬКО своих предметов
- ✅ Выставление оценок студентам
- ✅ Редактирование оценок
- ✅ Просмотр списка студентов
- ✅ Топ-10 студентов

### 👨‍🎓 Студент
- ✅ Просмотр своего профиля
- ✅ Просмотр своих предметов
- ✅ Просмотр своего учебного плана
- ✅ "Мой дневник" с оценками
- ✅ История оценок
- ✅ Средний рейтинг
- ✅ Топ-10 студентов

## 📋 Установка и запуск

### Предварительные требования
- Node.js >= 18.x
- PostgreSQL >= 14.x
- npm или yarn

### 1. Установка зависимостей

```bash
# Корневая директория
npm install

# Backend
cd backend
npm install

# Frontend
cd frontend
npm install
```

### 2. Настройка базы данных

```bash
cd backend

# Создайте файл .env
cp .env.example .env

# Отредактируйте .env и укажите свои данные PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/university_portal?schema=public"
# JWT_SECRET=your_super_secret_jwt_key
```

### 3. Миграция базы данных и заполнение тестовыми данными

```bash
cd backend

# Генерация Prisma Client
npx prisma generate

# Создание таблиц
npx prisma migrate dev --name init

# Заполнение базы тестовыми данными
npm run seed
```

### 4. Запуск приложения

#### Вариант 1: Запуск всего проекта (рекомендуется)
```bash
# В корневой директории
npm run dev
```

#### Вариант 2: Запуск Backend и Frontend отдельно

**Terminal 1 - Backend:**
```bash
cd backend
npm run dev
```

**Terminal 2 - Frontend:**
```bash
cd frontend
npm run dev
```

### 5. Доступ к приложению

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **Prisma Studio** (опционально): `cd backend && npx prisma studio`

## 🔑 Тестовые аккаунты

После выполнения `npm run seed` будут созданы следующие тестовые аккаунты:

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@university.uz | password123 |
| Преподаватель 1 | teacher1@university.uz | password123 |
| Преподаватель 2 | teacher2@university.uz | password123 |
| Студент 1 | student1@university.uz | password123 |
| Студент 2 | student2@university.uz | password123 |

## 📁 Структура проекта

```
university-portal/
├── backend/                  # Backend (Node.js + Express + Prisma)
│   ├── controllers/          # Контроллеры API
│   ├── middleware/           # Middleware (auth)
│   ├── prisma/              # Prisma схема и миграции
│   │   ├── schema.prisma    # Database schema
│   │   └── seed.js          # Seed data
│   ├── routes/              # API маршруты
│   ├── utils/               # Утилиты
│   ├── .env.example         # Пример конфигурации
│   ├── package.json
│   └── server.js            # Точка входа
│
├── frontend/                 # Frontend (React + Vite)
│   ├── src/
│   │   ├── components/      # Переиспользуемые компоненты
│   │   ├── layouts/         # Layout компоненты
│   │   ├── pages/           # Страницы
│   │   │   ├── admin/       # Страницы админа
│   │   │   ├── teacher/     # Страницы преподавателя
│   │   │   └── student/     # Страницы студента
│   │   ├── services/        # API сервисы
│   │   ├── store/           # Zustand store
│   │   ├── App.jsx          # Main App
│   │   ├── index.css        # Tailwind CSS
│   │   └── main.jsx         # Entry point
│   ├── index.html
│   ├── package.json
│   ├── tailwind.config.js
│   └── vite.config.js
│
├── package.json             # Root package.json
└── README.md                # Документация
```

## 🔒 Безопасность

- JWT токены с истечением срока действия
- Bcrypt для хеширования паролей
- Role-based access control (RBAC)
- Защищенные API endpoints
- Валидация данных

## 🎨 UI/UX

- Современный dashboard дизайн
- Адаптивный дизайн (Mobile-friendly)
- Красивые карточки и градиенты
- Плавные анимации
- Иконки Lucide React
- Диаграммы Chart.js
- Toast уведомления

## 📊 База данных

### Основные модели:
- **User** - Пользователи (с ролями)
- **Teacher** - Преподаватели
- **Student** - Студенты
- **Subject** - Предметы
- **Grade** - Оценки
- **StudyPlan** - Учебные планы
- **News** - Новости
- **Graduate** - Выпускники

## 🛠️ API Endpoints

### Authentication
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Teachers (Admin only)
- `GET /api/teachers` - Список преподавателей
- `POST /api/teachers` - Создать преподавателя
- `PUT /api/teachers/:id` - Обновить преподавателя
- `DELETE /api/teachers/:id` - Удалить преподавателя

### Students (Admin)
- `GET /api/students` - Список студентов
- `GET /api/students/top` - Топ-10 студентов
- `POST /api/students` - Создать студента
- `PUT /api/students/:id` - Обновить студента
- `DELETE /api/students/:id` - Удалить студента

### Subjects (Admin, Teacher)
- `GET /api/subjects` - Список предметов
- `POST /api/subjects` - Создать предмет
- `PUT /api/subjects/:id` - Обновить предмет
- `DELETE /api/subjects/:id` - Удалить предмет

### Grades (Admin, Teacher)
- `GET /api/grades` - Список оценок
- `GET /api/grades/student/:studentId` - Оценки студента
- `POST /api/grades` - Создать оценку
- `PUT /api/grades/:id` - Обновить оценку
- `DELETE /api/grades/:id` - Удалить оценку

### И другие...

## 🚧 Production Build

```bash
# Build frontend
cd frontend
npm run build

# Результат будет в frontend/dist
# Настройте nginx или другой web-сервер для раздачи статических файлов
```

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

University Portal Project

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

---

**Готово к production!** ✅
